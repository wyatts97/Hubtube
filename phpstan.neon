includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 5
    paths:
        - app
    # Existing findings live in phpstan-baseline.neon; new code must pass.
    reportUnmatchedIgnoredErrors: false
